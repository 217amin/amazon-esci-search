# Serving-only deps. Excludes training/eval cruft (mlflow, jupyter, matplotlib,
# datasets, pinecone-client). Keep this slim to keep Docker images small and
# Modal cold starts fast.

# Core
pandas>=1.5.0
numpy>=1.21.0
pyarrow>=10.0.0
PyYAML>=6.0

# DL / NLP
torch>=2.6.0
sentence-transformers>=3.0.0,<4.0.0
transformers>=4.44.0,<5.0.0

# Sparse retrieval
scikit-learn>=1.0.0
nltk>=3.8.1
rank-bm25>=0.2.2

# Vector search
faiss-cpu>=1.7.4

# API
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0